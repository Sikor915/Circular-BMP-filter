#include "pch.h"
#include "Circus.h"

void Circus(long long x, long long y)
{
	std::cout << "Circus function called with arguments: " << x << " and " << y << std::endl;
}

int CompressionFuncCircus(int x, int y)
{
	return x + y;
}

